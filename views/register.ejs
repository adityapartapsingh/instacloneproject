<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Register • Insta Clone</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body{font-family:Inter,system-ui,Arial,sans-serif;background:#fafafa;margin:0;display:flex;align-items:center;justify-content:center;height:100vh}
        .card{background:#fff;border:1px solid #e6e6e6;padding:28px;border-radius:8px;width:380px}
        .field{display:block;margin-bottom:12px}
        label{display:block;margin-bottom:6px;font-size:13px}
        input{width:100%;padding:10px;border:1px solid #dbdbdb;border-radius:6px}
        .btn{width:100%;padding:10px;border-radius:6px;background:#3897f0;color:#fff;border:0;cursor:pointer;margin-top:8px}
        .muted{text-align:center;color:#8e8e8e;font-size:14px;margin-top:12px}
        a{color:#3897f0}
    </style>
</head>
<body>
    <div class="card">
        <h2 style="text-align:center;margin:0 0 14px 0">Sign up</h2>
                <% if (typeof errors !== 'undefined' && errors && errors.length) { %>
                    <div style="background:#ffe6e6;color:#8b0000;padding:10px;border-radius:6px;margin-bottom:12px">
                        <% errors.forEach(function(err){ %>
                            <!DOCTYPE html>
                            <html lang="en">
                                <head>
                                    <meta charset="utf-8" />
                                    <meta name="viewport" content="width=device-width,initial-scale=1" />
                                    <title>Register • Insta Clone</title>
                                    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
                                    <style>
                                        body{font-family:Inter,system-ui,Arial,sans-serif;background:#fafafa;margin:0;display:flex;align-items:center;justify-content:center;height:100vh}
                                        .card{background:#fff;border:1px solid #e6e6e6;padding:28px;border-radius:8px;width:380px}
                                        .field{display:block;margin-bottom:12px}
                                        label{display:block;margin-bottom:6px;font-size:13px}
                                        input{width:100%;padding:10px;border:1px solid #dbdbdb;border-radius:6px}
                                        .btn{width:100%;padding:10px;border-radius:6px;background:#3897f0;color:#fff;border:0;cursor:pointer;margin-top:8px}
                                        .muted{text-align:center;color:#8e8e8e;font-size:14px;margin-top:12px}
                                        a{color:#3897f0}
                                    </style>
                                </head>
                                <body>
                                    <div class="card">
                                        <h2 style="text-align:center;margin:0 0 14px 0">Sign up</h2>
                                        <% if (errors && errors.length) { %>
                                            <div style="background:#ffe6e6;color:#8b0000;padding:10px;border-radius:6px;margin-bottom:12px">
                                                <% errors.forEach(function(err){ %>
                                                    <div><%= err %></div>
                                                <% }) %>
                                            </div>
                                        <% } %>
                                        <% if (success && success.length) { %>
                                            <div style="background:#e6ffef;color:#056608;padding:10px;border-radius:6px;margin-bottom:12px">
                                                <% success.forEach(function(msg){ %>
                                                    <div><%= msg %></div>
                                                <% }) %>
                                            </div>
                                        <% } %>
                                        <form action="/auth/register" method="POST">
                                            <div class="field">
                                                <label for="name">Name</label>
                                                <input id="name" name="name" required>
                                            </div>
                                            <div class="field">
                                                <label for="username">Username</label>
                                                <input id="username" name="username" required>
                                            </div>
                                            <div class="field">
                                                <label for="password">Password</label>
                                                <input id="password" name="password" type="password" required>
                                            </div>
                                            <button class="btn" type="submit">Sign up</button>
                                        </form>
                                        <div class="muted">Already have an account? <a href="/auth/login">Log in</a></div>
                                    </div>
                                </body>
                            </html>